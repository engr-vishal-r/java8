package interfaces;

public interface IBankNew {
    /**Interface is similar to an abstract class that contains only abstract methods, rather it is set of abstract methods.
     if a class is implementing an interface then all the methods of the particular interface must be overridden in that class.
     If not, that class should be declared a an abstract class
     *
     */

    boolean applyforCreditCard(Customer customer);
}
